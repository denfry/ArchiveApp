# 📤 Загрузка проекта на GitHub

## 🚀 Быстрая инструкция

### Шаг 1: Проверка Git

```bash
git --version
```

Если Git не установлен, скачайте с [git-scm.com](https://git-scm.com/)

### Шаг 2: Инициализация репозитория

```bash
cd C:\Users\denfry\PycharmProjects\Archive
git init
```

### Шаг 3: Добавление файлов

```bash
git add .
```

### Шаг 4: Первый коммит

```bash
git commit -m "Initial commit: Archive management system with QR codes"
```

### Шаг 5: Подключение к GitHub

```bash
git remote add origin https://github.com/denfry/ArchiveApp.git
```

### Шаг 6: Загрузка на GitHub

```bash
git branch -M main
git push -u origin main
```

Если потребуется авторизация:

- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи

---

## 📝 Подробная инструкция

### Подготовка

1. **Убедитесь, что Git установлен:**

   ```bash
   git --version
   ```

2. **Настройте Git (если еще не настроен):**

   ```bash
   git config --global user.name "Ваше Имя"
   git config --global user.email "ваш@email.com"
   ```

### Загрузка проекта

#### Вариант 1: Через HTTPS (проще)

1. **Инициализируйте репозиторий:**

   ```bash
   cd C:\Users\denfry\PycharmProjects\Archive
   git init
   ```

2. **Добавьте все файлы:**

   ```bash
   git add .
   ```

3. **Сделайте первый коммит:**

   ```bash
   git commit -m "Initial commit: Archive management system with QR codes and mobile app"
   ```

4. **Подключите удаленный репозиторий:**

   ```bash
   git remote add origin https://github.com/denfry/ArchiveApp.git
   ```

5. **Переименуйте ветку в main (если нужно):**

   ```bash
   git branch -M main
   ```

6. **Загрузите на GitHub:**

   ```bash
   git push -u origin main
   ```

   **Если потребуется авторизация:**
   - GitHub больше не принимает пароли
   - Используйте Personal Access Token:
     1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
     2. Generate new token (classic)
     3. Выберите права: `repo`
     4. Скопируйте токен
     5. Используйте токен как пароль при push

#### Вариант 2: Через SSH (рекомендуется для постоянной работы)

1. **Создайте SSH ключ (если нет):**

   ```bash
   ssh-keygen -t ed25519 -C "ваш@email.com"
   ```

   Нажмите Enter для всех вопросов.

2. **Добавьте ключ в SSH агент:**

   ```bash
   # Windows (Git Bash)
   eval "$(ssh-agent -s)"
   ssh-add ~/.ssh/id_ed25519
   ```

3. **Скопируйте публичный ключ:**

   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

   Скопируйте весь вывод.

4. **Добавьте ключ на GitHub:**
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте скопированный ключ
   - Сохраните

5. **Используйте SSH URL:**

   ```bash
   git remote add origin git@github.com:denfry/ArchiveApp.git
   git push -u origin main
   ```

---

## 🔄 Обновление проекта на GitHub

После внесения изменений:

```bash
# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

---

## 📋 Что будет загружено

Следующие файлы будут загружены:

- ✅ Все Python файлы (.py)
- ✅ Конфигурационные файлы (requirements.txt, Procfile, etc.)
- ✅ Документация (.md файлы)
- ✅ Манифест PWA (manifest.json)
- ✅ Иконки (icon-*.png)

Следующие файлы НЕ будут загружены (благодаря .gitignore):

- ❌ База данных (data/*.db)
- ❌ Логи (logs/*)
- ❌ Временные файлы (**pycache**, *.pyc)
- ❌ Виртуальное окружение (venv/)
- ❌ Скомпилированные файлы (dist/, build/)

---

## 🛠️ Решение проблем

### Проблема: "fatal: not a git repository"

**Решение:**

```bash
git init
```

### Проблема: "remote origin already exists"

**Решение:**

```bash
git remote remove origin
git remote add origin https://github.com/denfry/ArchiveApp.git
```

### Проблема: "Authentication failed"

**Решение:**

1. Используйте Personal Access Token вместо пароля
2. Или настройте SSH ключи (см. выше)

### Проблема: "Permission denied"

**Решение:**

1. Убедитесь, что у вас есть доступ к репозиторию
2. Проверьте правильность URL репозитория
3. Используйте Personal Access Token с правами `repo`

---

## ✅ Проверка загрузки

После успешной загрузки:

1. Откройте <https://github.com/denfry/ArchiveApp>
2. Убедитесь, что все файлы загружены
3. Проверьте, что README.md отображается правильно

---

## 🎯 Следующие шаги

После загрузки на GitHub:

1. **Развертывание на Railway:**
   - Railway может автоматически развернуть из GitHub
   - Выберите "Deploy from GitHub repo" при создании сервиса

2. **Настройка GitHub Pages (опционально):**
   - Можно разместить документацию на GitHub Pages

3. **Добавление описания репозитория:**
   - На странице репозитория нажмите ⚙️ Settings
   - Добавьте описание проекта

---

**Готово!** Ваш проект теперь на GitHub! 🎉

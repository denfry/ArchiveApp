# 🔧 Исправление проблемы с localhost в QR-кодах

## ❌ Проблема

При сканировании QR-кода на телефоне открывается `localhost`, который не работает на телефоне.

**Причина:** QR-коды были сгенерированы с `localhost` вместо URL вашего облачного сервера.

## ✅ Решение

Нужно перегенерировать наклейки с правильным URL.

### Шаг 1: Узнайте URL вашего развернутого сайта

Найдите URL в настройках вашего облачного хостинга:

- **Railway:** В настройках сервиса → Networking → Domain
- **Render:** В настройках сервиса → Environment → Public URL
- **Heroku:** `https://your-app-name.herokuapp.com`

Пример: `https://archive-app.up.railway.app`

### Шаг 2: Установите BASE_URL

#### Вариант A: Через скрипт (проще)

**Windows (CMD):**
```bash
set_base_url.bat
```

**PowerShell:**
```powershell
.\set_base_url.ps1
```

Введите URL вашего сайта, когда скрипт попросит.

#### Вариант B: Вручную

**Windows (CMD):**
```bash
set BASE_URL=https://your-app.railway.app
```

**PowerShell:**
```powershell
$env:BASE_URL = "https://your-app.railway.app"
```

**Linux/Mac:**
```bash
export BASE_URL=https://your-app.railway.app
```

### Шаг 3: Перегенерируйте наклейки

1. **Закройте приложение** (если оно открыто)

2. **Запустите приложение с установленной переменной:**

   **Windows (CMD):**
   ```bash
   set BASE_URL=https://your-app.railway.app
   python main.py
   ```

   **PowerShell:**
   ```powershell
   $env:BASE_URL = "https://your-app.railway.app"
   python main.py
   ```

   **Linux/Mac:**
   ```bash
   export BASE_URL=https://your-app.railway.app
   python main.py
   ```

3. **В приложении:**
   - Просмотр архива → Наклейки
   - Выберите те же коробки
   - Нажмите "Печатать"
   - Новый PDF будет в папке `exports/`

4. **Распечатайте новые наклейки** с правильными QR-кодами

## 🔍 Проверка

После генерации:

1. Откройте PDF с наклейками
2. Отсканируйте QR-код (можно через приложение на телефоне)
3. Должен открыться ваш облачный сайт, а не localhost

## 💡 Постоянное решение

Чтобы не устанавливать BASE_URL каждый раз:

### Windows (постоянная установка):

```bash
setx BASE_URL "https://your-app.railway.app"
```

Затем перезапустите терминал и запустите приложение.

### Linux/Mac (добавить в ~/.bashrc или ~/.zshrc):

```bash
echo 'export BASE_URL="https://your-app.railway.app"' >> ~/.bashrc
source ~/.bashrc
```

## 🚨 Важно

- **Старые наклейки** с localhost работать не будут - их нужно перепечатать
- **Новые наклейки** будут работать правильно
- URL должен быть **HTTPS** (не HTTP) для работы камеры на телефоне

## 📝 Пример полного процесса

```bash
# 1. Установить BASE_URL
set BASE_URL=https://archive-app.up.railway.app

# 2. Запустить приложение
python main.py

# 3. В приложении: Просмотр → Наклейки → Печатать

# 4. Распечатать новые наклейки
```

---

**Готово!** Теперь QR-коды будут работать на телефоне! 📱✅

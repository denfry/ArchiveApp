# 📚 Полный гайд по запуску проекта "Архив документов"

## 📋 Содержание

1. [Требования](#требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Первоначальная настройка](#первоначальная-настройка)
4. [Запуск приложения](#запуск-приложения)
5. [Запуск веб-сервера](#запуск-веб-сервера)
6. [Развертывание в облаке](#развертывание-в-облаке)
7. [Настройка QR-кодов](#настройка-qr-кодов)
8. [Установка на телефон](#установка-на-телефон)
9. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

### Необходимое ПО

- **Python 3.9+** (рекомендуется 3.10 или 3.11)
- **pip** (менеджер пакетов Python)
- **Git** (для развертывания в облаке, опционально)

### Проверка установки

```bash
# Проверка Python
python --version
# Должно показать: Python 3.9.x или выше

# Проверка pip
pip --version
```

---

## 📦 Установка зависимостей

### Шаг 1: Откройте терминал в папке проекта

```bash
cd C:\Users\denfry\PycharmProjects\Archive
```

### Шаг 2: Создайте виртуальное окружение (рекомендуется)

**Windows:**

```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**

```bash
python3 -m venv venv
source venv/bin/activate
```

После активации в начале строки терминала появится `(venv)`.

### Шаг 3: Установите все зависимости

```bash
pip install -r requirements.txt
```

Если файла `requirements.txt` нет, установите вручную:

```bash
pip install PyQt6 openpyxl reportlab qrcode[pil] Pillow
```

### Шаг 4: Проверка установки

```bash
python -c "import PyQt6; import openpyxl; import reportlab; import qrcode; print('Все модули установлены!')"
```

---

## ⚙️ Первоначальная настройка

### Шаг 1: Создание структуры папок

Приложение автоматически создаст необходимые папки при первом запуске:

- `data/` - база данных
- `logs/` - логи приложения
- `exports/` - экспортированные файлы

### Шаг 2: Создание иконок для PWA (опционально)

Если у вас есть файл `icon.png`, создайте иконки для мобильного приложения:

```bash
python create_icons.py
```

Это создаст файлы:

- `icon-192.png` (192x192 пикселей)
- `icon-512.png` (512x512 пикселей)

---

## 🚀 Запуск приложения

### Вариант 1: Запуск основного приложения (десктоп)

```bash
python main.py
```

**Что происходит:**

- Открывается главное окно приложения
- Создается база данных `data/archive.db` (если её нет)
- Можно добавлять, редактировать, просматривать документы

### Вариант 2: Запуск веб-сервера (для QR-кодов)

В **отдельном терминале** запустите:

```bash
python web_server.py
```

**Что происходит:**

- Запускается веб-сервер на порту 8080
- Доступен по адресу: `http://localhost:8080`
- Можно открыть в браузере для тестирования

**Проверка работы:**

1. Откройте браузер
2. Перейдите на `http://localhost:8080`
3. Должна открыться главная страница
4. Перейдите на `http://localhost:8080/scanner` - откроется сканер QR-кодов

---

## 🌐 Развертывание в облаке

### Вариант 1: Railway (Рекомендуется - самый простой)

> **📖 Подробная инструкция:** См. [RAILWAY_SETUP.md](RAILWAY_SETUP.md) для детального руководства по развертыванию на Railway.

#### Способ A: Через веб-интерфейс (Рекомендуется)

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите через GitHub или создайте аккаунт

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo" (если код на GitHub)
   - Или выберите "Empty Project" для ручного развертывания

3. **Добавьте сервис:**
   - В проекте нажмите "+ New"
   - Выберите "GitHub Repo" (если используете GitHub)
   - Или выберите "Empty Service" и загрузите файлы

4. **Настройте сервис:**
   - Railway автоматически определит Python
   - Убедитесь, что выбран файл `web_server.py` как точка входа
   - В настройках "Settings" → "Deploy" проверьте:
     - **Start Command:** `python web_server.py`
     - **Build Command:** оставьте пустым (Railway установит зависимости автоматически)

5. **Настройте переменные окружения:**
   - Перейдите в "Variables"
   - Добавьте переменную:
     - **Name:** `BASE_URL`
     - **Value:** `https://your-project-name.up.railway.app`
   - Добавьте переменную (опционально):
     - **Name:** `PORT`
     - **Value:** оставьте пустым (Railway установит автоматически)

6. **Получите URL:**
   - В настройках сервиса найдите раздел "Networking"
   - Railway автоматически создаст домен вида: `your-project-name.up.railway.app`
   - Скопируйте этот URL

#### Способ B: Через CLI

1. **Установка Railway CLI:**

   ```bash
   npm install -g @railway/cli
   ```

   Если у вас нет Node.js, установите его с [nodejs.org](https://nodejs.org/)

2. **Вход в Railway:**

   ```bash
   railway login
   ```

   Откроется браузер для авторизации.

3. **Создание проекта:**

   ```bash
   railway init
   ```

   Выберите:

   - "Create a new project"
   - Введите название проекта (например: `archive-app`)

4. **Создание сервиса (если нужно):**

   ```bash
   railway service
   ```

   Или создайте сервис через веб-интерфейс.

5. **Настройка переменных окружения:**

   ```bash
   railway variables set BASE_URL=https://your-project-name.up.railway.app
   ```

   **Важно:** Замените `your-project-name` на реальное имя вашего проекта Railway.

6. **Развертывание:**

   ```bash
   railway up
   ```

   Railway автоматически:

   - Определит Python
   - Установит зависимости из `requirements.txt`
   - Запустит `web_server.py`

7. **Получение URL:**

   ```bash
   railway domain
   ```

   Или в веб-интерфейсе Railway найдите URL вашего приложения.

#### Важные файлы для Railway

Убедитесь, что в проекте есть следующие файлы:

- ✅ `requirements.txt` - зависимости Python
- ✅ `Procfile` или `railway.json` - конфигурация запуска
- ✅ `runtime.txt` - версия Python (опционально)
- ✅ `web_server.py` - главный файл веб-сервера

---

### Вариант 2: Render

#### Шаг 1: Регистрация

1. Зайдите на [render.com](https://render.com)
2. Зарегистрируйтесь (можно через GitHub)

#### Шаг 2: Создание Web Service

1. Нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий (или создайте новый)
3. Выберите репозиторий с проектом

#### Шаг 3: Настройка

**Build Command:**

```bash
pip install -r requirements.txt
```

**Start Command:**

```bash
python web_server.py
```

**Environment:**

- Python 3

#### Шаг 4: Переменные окружения

В разделе "Environment":

- `BASE_URL` = `https://your-app-name.onrender.com`
- `PORT` = оставьте пустым (Render установит автоматически)

#### Шаг 5: Развертывание

Нажмите "Create Web Service" - Render начнет развертывание.

---

### Вариант 3: Heroku

#### Шаг 1: Установка Heroku CLI

Скачайте с [devcenter.heroku.com/articles/heroku-cli](https://devcenter.heroku.com/articles/heroku-cli)

#### Шаг 2: Вход

```bash
heroku login
```

#### Шаг 3: Создание приложения

```bash
heroku create your-app-name
```

#### Шаг 4: Настройка переменных

```bash
heroku config:set BASE_URL=https://your-app-name.herokuapp.com
```

#### Шаг 5: Развертывание

```bash
git init
git add .
git commit -m "Initial commit"
git push heroku main
```

---

## 🔗 Настройка QR-кодов

### Для локального использования

QR-коды по умолчанию используют `http://localhost:8080`. Это работает только на вашем компьютере.

### Для использования на телефоне

**Вариант 1: Через облачный сервер (рекомендуется)**

1. Разверните веб-сервер в облаке (Railway, Render и т.д.)
2. Получите URL (например: `https://your-app.railway.app`)
3. Установите переменную окружения перед запуском приложения:

**Windows:**

```bash
set BASE_URL=https://your-app.railway.app
python main.py
```

**Linux/Mac:**

```bash
export BASE_URL=https://your-app.railway.app
python main.py
```

1. Сгенерируйте наклейки - QR-коды будут содержать правильный URL

**Вариант 2: Через ngrok (для тестирования)**

1. Установите ngrok: [ngrok.com/download](https://ngrok.com/download)
2. Запустите веб-сервер локально:

   ```bash
   python web_server.py
   ```

3. В другом терминале запустите ngrok:

   ```bash
   ngrok http 8080
   ```

4. Скопируйте URL от ngrok (например: `https://abc123.ngrok.io`)
5. Установите переменную окружения:

   ```bash
   set BASE_URL=https://abc123.ngrok.io
   ```

6. Запустите приложение и сгенерируйте наклейки

---

## 📱 Установка на телефон

### Шаг 1: Разверните веб-сервер в облаке

Следуйте инструкциям выше для Railway, Render или Heroku.

### Шаг 2: Откройте сайт на телефоне

1. Откройте браузер на телефоне (Chrome, Safari)
2. Перейдите на URL вашего развернутого приложения
   - Например: `https://your-app.railway.app`

### Шаг 3: Установка PWA

**Android (Chrome):**

1. Откройте меню браузера (три точки)
2. Выберите "Добавить на главный экран" или "Установить приложение"
3. Подтвердите установку
4. Иконка появится на главном экране

**iOS (Safari):**

1. Нажмите кнопку "Поделиться" (квадрат со стрелкой)
2. Выберите "На экран «Домой»"
3. Нажмите "Добавить"
4. Иконка появится на главном экране

### Шаг 4: Использование

1. Откройте приложение с главного экрана
2. Перейдите в "Сканер QR-кодов"
3. Разрешите доступ к камере
4. Наведите на QR-код с наклейки
5. Автоматически откроется информация о коробке

---

## 🎯 Полный рабочий процесс

### 1. Первый запуск (настройка)

```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Создание иконок (если есть icon.png)
python create_icons.py

# 3. Запуск основного приложения
python main.py
```

### 2. Добавление данных

1. В приложении добавьте коробки и документы
2. Данные сохраняются в `data/archive.db`

### 3. Развертывание веб-сервера

```bash
# Вариант A: Локально (для тестирования)
python web_server.py

# Вариант B: В облаке (Railway)
railway login
railway init
railway variables set BASE_URL=your-app.railway.app
railway up
```

### 4. Генерация наклеек с QR-кодами

```bash
# Установите BASE_URL перед запуском
set BASE_URL=https://your-app.railway.app  # Windows
# или
export BASE_URL=https://your-app.railway.app  # Linux/Mac

# Запустите приложение
python main.py

# В приложении:
# 1. Просмотр архива → Наклейки
# 2. Выберите коробки
# 3. Выберите раскладку (рекомендуется 6x8 = 48 наклеек)
# 4. Нажмите "Печатать"
# 5. PDF будет в папке exports/
```

### 5. Использование на телефоне

1. Установите PWA на телефон (см. выше)
2. Откройте сканер QR-кодов
3. Отсканируйте QR-код с наклейки
4. Просмотрите информацию о коробке и документах

---

## 🔍 Решение проблем

### Проблема: "ModuleNotFoundError"

**Решение:**

```bash
pip install -r requirements.txt
```

### Проблема: Порт 8080 занят

**Решение:**
Измените порт в `web_server.py`:

```python
def start_web_server(port=8081):  # Измените на другой порт
```

Или убейте процесс, занимающий порт:

```bash
# Windows
netstat -ano | findstr :8080
taskkill /PID <номер_процесса> /F

# Linux/Mac
lsof -ti:8080 | xargs kill
```

### Проблема: QR-коды не открываются на телефоне

**Причины:**

1. Веб-сервер не развернут в облаке
2. Неправильный BASE_URL
3. Проблемы с интернетом

**Решение:**

1. Убедитесь, что веб-сервер развернут и доступен
2. Проверьте BASE_URL перед генерацией PDF
3. Протестируйте URL в браузере на телефоне

### Проблема: Камера не работает в сканере

**Решение:**

1. Убедитесь, что сайт открыт по HTTPS (обязательно для камеры)
2. Разрешите доступ к камере в настройках браузера
3. Используйте современный браузер (Chrome, Safari, Firefox)

### Проблема: База данных не создается

**Решение:**

1. Убедитесь, что папка `data/` существует и доступна для записи
2. Проверьте права доступа к файлам
3. Запустите приложение от имени администратора (если нужно)

### Проблема: Ошибки при развертывании на Railway

**Решение:**

1. Убедитесь, что `requirements.txt` содержит все зависимости
2. Проверьте логи в Railway Dashboard
3. Убедитесь, что `web_server.py` запускается локально

---

## 📞 Дополнительная помощь

### Полезные команды

```bash
# Проверка версии Python
python --version

# Список установленных пакетов
pip list

# Обновление пакетов
pip install --upgrade -r requirements.txt

# Просмотр логов приложения
# Windows
type logs\archive.log
# Linux/Mac
cat logs/archive.log

# Проверка работы веб-сервера
curl http://localhost:8080
```

### Структура проекта

```
Archive/
├── main.py                 # Главный файл приложения
├── web_server.py           # Веб-сервер для QR-кодов
├── data_manager.py         # Работа с базой данных
├── view_window.py          # Окно просмотра
├── dialogs.py              # Диалоговые окна
├── models.py               # Модели данных
├── requirements.txt        # Зависимости
├── manifest.json          # PWA манифест
├── Procfile               # Для Heroku
├── vercel.json            # Для Vercel
├── data/                  # База данных (создается автоматически)
├── logs/                  # Логи (создается автоматически)
└── exports/               # Экспортированные файлы
```

---

## ✅ Чек-лист готовности

- [ ] Python 3.9+ установлен
- [ ] Все зависимости установлены (`pip install -r requirements.txt`)
- [ ] Приложение запускается (`python main.py`)
- [ ] Веб-сервер запускается локально (`python web_server.py`)
- [ ] Сайт открывается в браузере (`http://localhost:8080`)
- [ ] Сканер QR-кодов работает
- [ ] Веб-сервер развернут в облаке
- [ ] BASE_URL настроен правильно
- [ ] QR-коды генерируются с правильным URL
- [ ] PWA установлено на телефон
- [ ] Сканирование QR-кодов работает на телефоне

---

**Готово!** Теперь у вас есть полностью рабочая система управления архивом с мобильным приложением! 🎉

# 🚂 Развертывание на Railway - Подробная инструкция

## 📋 Что такое Railway Service?

Railway требует создания **Service** (сервиса) в проекте. Сервис - это отдельное приложение или компонент, который Railway будет запускать и развертывать.

## 🎯 Пошаговая инструкция

### Шаг 1: Регистрация и вход

1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub (рекомендуется) или создайте аккаунт
3. Подтвердите email, если требуется

### Шаг 2: Создание проекта

1. На главной странице нажмите **"New Project"**
2. Выберите один из вариантов:
   - **"Deploy from GitHub repo"** - если ваш код уже на GitHub
   - **"Empty Project"** - для ручного развертывания

### Шаг 3: Создание сервиса (Service)

**Важно:** Railway требует создать Service для запуска приложения!

#### Вариант A: Если код на GitHub

1. После создания проекта нажмите **"+ New"** или **"Add Service"**
2. Выберите **"GitHub Repo"**
3. Выберите ваш репозиторий с проектом
4. Railway автоматически:
   - Определит Python
   - Найдет `requirements.txt`
   - Начнет развертывание

#### Вариант B: Если код локально (через CLI)

1. Установите Railway CLI:
   ```bash
   npm install -g @railway/cli
   ```

2. Войдите:
   ```bash
   railway login
   ```

3. В папке проекта:
   ```bash
   railway init
   ```

4. Создайте сервис:
   ```bash
   railway service
   ```

   Или через веб-интерфейс: нажмите "+ New" → "Empty Service"

5. Загрузите файлы:
   ```bash
   railway up
   ```

### Шаг 4: Настройка сервиса

1. Откройте созданный сервис в Railway Dashboard
2. Перейдите в **"Settings"** → **"Deploy"**
3. Проверьте настройки:
   - **Start Command:** `python web_server.py`
   - **Build Command:** оставьте пустым (Railway установит зависимости автоматически)
   - **Root Directory:** оставьте пустым (если проект в корне)

### Шаг 5: Настройка переменных окружения

1. В настройках сервиса перейдите в **"Variables"**
2. Добавьте переменные:

   **BASE_URL:**
   - **Name:** `BASE_URL`
   - **Value:** `https://your-project-name.up.railway.app`
   - **Примечание:** Замените `your-project-name` на реальное имя вашего проекта
   - **Как узнать URL:** См. Шаг 6

   **PORT (опционально):**
   - Railway автоматически установит переменную `PORT`
   - Не нужно добавлять вручную

### Шаг 6: Получение URL вашего приложения

1. В настройках сервиса перейдите в **"Networking"** или **"Settings"** → **"Networking"**
2. Railway автоматически создаст домен вида:
   - `your-project-name.up.railway.app`
3. Скопируйте этот URL
4. Обновите переменную `BASE_URL` с правильным URL (если еще не сделали)

### Шаг 7: Проверка развертывания

1. В разделе **"Deployments"** вы увидите процесс развертывания
2. Дождитесь завершения (обычно 2-5 минут)
3. Нажмите на домен для открытия приложения
4. Должна открыться главная страница

## 🔧 Решение проблем

### Проблема: "No service found" или "Service required"

**Решение:**
1. Убедитесь, что вы создали Service в проекте
2. Нажмите "+ New" → "Empty Service" или "GitHub Repo"
3. Выберите правильный репозиторий/загрузите файлы

### Проблема: Railway не определяет Python

**Решение:**
1. Убедитесь, что в проекте есть `requirements.txt`
2. Убедитесь, что есть файл `web_server.py`
3. В настройках сервиса укажите:
   - **Start Command:** `python web_server.py`

### Проблема: Ошибки при установке зависимостей

**Решение:**
1. Проверьте `requirements.txt` - все зависимости должны быть указаны
2. Проверьте логи развертывания в Railway Dashboard
3. Убедитесь, что используется правильная версия Python (см. `runtime.txt`)

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте логи в Railway Dashboard → "Deployments" → выберите последний deployment → "View Logs"
2. Убедитесь, что `web_server.py` запускается локально
3. Проверьте, что порт берется из переменной окружения `PORT`

## 📝 Структура файлов для Railway

Убедитесь, что в корне проекта есть:

```
Archive/
├── web_server.py      # ✅ Главный файл веб-сервера
├── requirements.txt   # ✅ Зависимости Python
├── Procfile          # ✅ Команда запуска (опционально)
├── railway.json      # ✅ Конфигурация Railway (опционально)
├── runtime.txt       # ✅ Версия Python (опционально)
├── data_manager.py   # ✅ Модуль работы с БД
├── manifest.json     # ✅ PWA манифест
└── ...
```

## 🎯 Быстрая проверка

После развертывания проверьте:

1. ✅ Сервис создан в проекте
2. ✅ Переменная `BASE_URL` установлена
3. ✅ Приложение доступно по URL
4. ✅ Главная страница открывается
5. ✅ Сканер QR-кодов работает (`/scanner`)

## 💡 Полезные команды CLI

```bash
# Вход в Railway
railway login

# Создание проекта
railway init

# Создание сервиса
railway service

# Просмотр сервисов
railway service list

# Установка переменных
railway variables set BASE_URL=https://your-app.up.railway.app

# Просмотр переменных
railway variables

# Развертывание
railway up

# Просмотр логов
railway logs

# Просмотр URL
railway domain
```

## 🔗 Дополнительные ресурсы

- [Документация Railway](https://docs.railway.app/)
- [Railway Discord](https://discord.gg/railway)
- [Примеры развертывания Python](https://docs.railway.app/guides/python)

---

**Готово!** Теперь ваш веб-сервер должен быть развернут на Railway! 🚂
